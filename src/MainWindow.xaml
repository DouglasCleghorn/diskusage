<Window x:Class="diskusage.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="diskusage"
        Icon="diskusage.ico"
        Width="1180"
        Height="720"
        MinWidth="760"
        MinHeight="480"
        Background="#F5F7FA"
        Foreground="#1F2937"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        FontSize="13"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        Loaded="Window_Loaded"
        Closing="Window_Closing">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="PanelRaisedBrush" Color="#F2F4F7" />
        <SolidColorBrush x:Key="BorderBrush" Color="#D0D5DD" />
        <SolidColorBrush x:Key="MutedBrush" Color="#667085" />
        <SolidColorBrush x:Key="AccentBrush" Color="#2563EB" />

        <Style x:Key="CellText" TargetType="TextBlock">
            <Setter Property="Margin" Value="10,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>

        <Style x:Key="RightAlignedCell" TargetType="TextBlock" BasedOn="{StaticResource CellText}">
            <Setter Property="TextAlignment" Value="Right" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="#344054" />
            <Setter Property="BorderBrush" Value="#D0D5DD" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F2F4F7" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E4E7EC" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="#101828" />
            <Setter Property="CaretBrush" Value="#101828" />
            <Setter Property="BorderBrush" Value="#D0D5DD" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="9,6" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F8FAFC" />
            <Setter Property="Foreground" Value="#475467" />
            <Setter Property="BorderBrush" Value="#D0D5DD" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="RightAlignedHeader" TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
            <Setter Property="HorizontalContentAlignment" Value="Right" />
        </Style>

        <Style x:Key="CenteredHeader" TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="#344054" />
            <Setter Property="BorderBrush" Value="#EAECF0" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Height" Value="30" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F9FAFB" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E8F1FC" />
                    <Setter Property="Foreground" Value="#175CD3" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="18,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,18,0">
                    <TextBlock Text="DISKUSAGE" FontSize="17" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" />
                    <TextBlock Text="storage explorer" Foreground="{StaticResource MutedBrush}" FontSize="11" />
                </StackPanel>
                <TextBox x:Name="PathBox" Grid.Column="1" MinWidth="260" KeyDown="PathBox_KeyDown" />
                <Button Grid.Column="2" Margin="8,0,0,0" Content="Browse…" Click="Browse_Click" />
                <Button x:Name="ScanButton" Grid.Column="3" Margin="8,0,0,0" Content="Scan" Click="Scan_Click" />
                <StackPanel Grid.Column="4" Orientation="Horizontal" Margin="10,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Level" Margin="0,0,5,0" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" />
                    <TextBox x:Name="CompressionLevelBox"
                             Width="38"
                             Padding="5,6"
                             ToolTip="Compression level: Parquet Zstd 1–3 → level 1, 4–7 → level 3 (blank default), 8–9 → level 19; Brotli 0–11; gzip/ZIP 0–9" />
                </StackPanel>
                <Button x:Name="SaveButton" Grid.Column="5" Margin="8,0,0,0" Content="Save…" IsEnabled="False" Click="Save_Click" />
                <Button x:Name="CancelButton" Grid.Column="6" Margin="8,0,0,0" Content="Cancel" IsEnabled="False" Click="Cancel_Click" />
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource PanelRaisedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="18,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="260" />
                </Grid.ColumnDefinitions>
                <Button x:Name="UpButton" Grid.Column="0" Margin="0,0,12,0" Padding="9,4" IsEnabled="False" Click="Up_Click">
                    <StackPanel Orientation="Horizontal">
                        <Path Width="12"
                              Height="12"
                              Margin="0,0,6,0"
                              Data="M 1,10 L 6,3 L 11,10"
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                              StrokeThickness="1.7"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"
                              Stretch="Uniform" />
                        <TextBlock Text="Up" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <TextBlock x:Name="CurrentPathText" Grid.Column="1" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Foreground="#344054" />
                <Border x:Name="ScanStateBadge" Grid.Column="2" Margin="12,0,0,0" Padding="8,3" CornerRadius="10" Background="#E4E7EC" VerticalAlignment="Center">
                    <TextBlock x:Name="ScanStateText" Text="READY" Foreground="#475467" FontSize="10" FontWeight="SemiBold" />
                </Border>
                <DockPanel Grid.Column="3" Margin="14,0,0,0">
                    <TextBlock Text="Filter" Foreground="{StaticResource MutedBrush}" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <TextBox x:Name="FilterBox" TextChanged="FilterBox_TextChanged" />
                </DockPanel>
            </Grid>
        </Border>

        <DataGrid x:Name="UsageGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  BorderThickness="0"
                  Background="#FFFFFF"
                  RowHeaderWidth="0"
                  SelectionMode="Single"
                  IsReadOnly="True"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True"
                  PreviewMouseDown="UsageGrid_PreviewMouseInput"
                  PreviewMouseUp="UsageGrid_PreviewMouseInput"
                  PreviewMouseWheel="UsageGrid_PreviewMouseInput"
                  MouseDoubleClick="UsageGrid_MouseDoubleClick"
                  KeyDown="UsageGrid_KeyDown">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Open" Click="Open_Click" />
                    <MenuItem Header="Show in File Explorer" Click="ShowInExplorer_Click" />
                    <Separator />
                    <MenuItem Header="Copy path" Click="CopyPath_Click" />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Name" Width="*" MinWidth="260">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid Margin="10,0" VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Width="16" Height="16" Source="{Binding Icon}" Stretch="Uniform" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Margin="8,0,0,0" Text="{Binding Name}" TextTrimming="CharacterEllipsis" ToolTip="{Binding FullPath}" VerticalAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Size" Binding="{Binding DisplaySize}" Width="100" ElementStyle="{StaticResource RightAlignedCell}" HeaderStyle="{StaticResource RightAlignedHeader}" />
                <DataGridTemplateColumn Header="Share" Width="220" HeaderStyle="{StaticResource CenteredHeader}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid Margin="10,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="48" />
                                </Grid.ColumnDefinitions>
                                <ProgressBar Grid.Column="0" Height="12" Margin="0,0,10,0" Maximum="100" Value="{Binding Percentage, Mode=OneWay}" Background="#E4E7EC" Foreground="#2563EB" BorderThickness="0" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Text="{Binding DisplayPercentage}" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="11" Foreground="#475467" />
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Files" Binding="{Binding DisplayFileCount}" Width="90" ElementStyle="{StaticResource RightAlignedCell}" HeaderStyle="{StaticResource RightAlignedHeader}" />
                <DataGridTextColumn Header="Details" Binding="{Binding DisplayDetails}" Width="150" ElementStyle="{StaticResource CellText}" />
                <DataGridTextColumn Header="Type" Binding="{Binding Kind}" Width="100" ElementStyle="{StaticResource CellText}" />
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="18,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText" Grid.Column="0" Foreground="{StaticResource MutedBrush}" Text="Ready" TextTrimming="CharacterEllipsis" />
                <TextBlock x:Name="SummaryText" Grid.Column="1" Foreground="#344054" Margin="20,0,0,0" />
            </Grid>
        </Border>
    </Grid>
</Window>
